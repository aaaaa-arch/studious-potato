# 安装教程

## 方式一：Git 安装（推荐）

1. 打开 Gopeed → 扩展页面
2. 开启右上角「开发者模式」
3. 点击「从 Git 安装」（连点下载图标 5 次可呼出）
4. 输入仓库地址：`https://github.com/aaaaa-arch/studious-potato`
5. 等待安装完成

## 方式二：本地安装

1. 下载本项目的 zip 文件
2. 解压到手机任意目录
3. Gopeed → 扩展 → 开启开发者模式
4. 点击「安装本地扩展」→ 选择解压后的文件夹

## 配置 Cookie

安装完成后：

1. Gopeed → 扩展 → 找到「全网盘直链下载器」
2. 点击齿轮图标 ⚙️ 进入设置
3. 填写对应网盘的 Cookie/Token
4. 点击保存

### 夸克 Cookie 获取步骤

1. 手机/电脑浏览器打开 `https://pan.quark.cn/` 并登录
2. 按 F12 打开开发者工具
3. 切到 Network（网络）标签
4. 刷新页面，找到任意 `list` 或 `detail` 请求
5. 右键 → Copy as cURL → 从中提取 `Cookie:` 后面的值
6. 粘贴到 Gopeed 扩展设置的「夸克网盘 Cookie」栏

### UC Cookie 获取

同上，只是网址换成 `https://drive.uc.cn/`

## 使用

1. 复制任意支持的网盘分享链接
2. 在 Gopeed 中新建下载任务
3. 粘贴链接 → 自动解析 → 开始下载
4. 下载完成后自动删除转存（默认 30 秒后）

## 常见问题

**Q: 配置页打不开？**
A: 检查 `manifest.json` 的 `settings` 字段格式，每个 setting 的 `type` 必须是 `string`/`number`/`boolean` 之一。

**Q: 夸克链接解析出来文件名还是哈希？**
A: 确认 Cookie 有效，且 `__puus` 值在 Cookie 中。过期了重新获取一次。

**Q: 下载完转存没删？**
A: 检查对应网盘的「自动删除转存」开关是否开启，以及延迟时间是否设置合理。

**Q: 蓝奏云要不要填 Cookie？**
A: 不用，蓝奏云解析是页面提取，无需登录。但带密码的分享暂时不支持。
